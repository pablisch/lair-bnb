 <%# <% @spaces.each do |space| %>
          <div class="space">
            <h1><%= space.name %></h1>
            <p><%= space.description %></p>
            <p>£<%= space.price %></p>
            <p>Available from <%= space.available_from %> to <%= space.available_to %></p>
          </div>
        <% end %> %>


        <p>session email = <%= session[:email] %></p>