<!DOCTYPE html>
<html>

<head>
  <title>MakersBnB</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>

  <nav>
    <div class="container centre nav-height">
      <div class="logo">
        <img src="bnb.png" alt="Flair BnB logo">
        <h1>Bookings - Made by me</h1>
      </div>
      <div class="links">
      <div class="logout navlink"><p><a href="/">Home</a></p></div>
          <div class="logout navlink"><p><a href="/logout">logout</a></p></div>
          <div class="new_space navlink"><p><a href="/new_space">Create Space</a></p></div>
      
      </div>
    </div>
  </nav>
  <div class="line"></div>
  <div class="container status">
  </div>

  <header>
        <div class="centre container">

          <div class="column">
            <div class="panels">
            <h3>Confirmed Bookings</h3>
              <% @confirmed_bookings.each do |booking| %>
              <div class="space">
                <h2><a href = "/spaces/<%= booking.space_id %>"><%= booking.name %></a></h2>
                <p>£<%= booking.price %></p>
                <p>Booking confirmed for <%= booking.booking_date %></p>
              </div>
              <% end %>
            </div>
          </div>

          <div class="column">
            <div class="panels">
            <h3>Pending Bookings</h3>
              <% @pending_bookings.each do |booking| %>
              <div class="space">
                <h2><a href = "/spaces/<%= booking.space_id %>"><%= booking.name %></a></h2>
                <p>£<%= booking.price %></p>
                <p>Booking pending for <%= booking.booking_date %></p>
              </div>
              <% end %>
            </div>
          </div>

          <div class="column">
            <div class="panels">
            <h3>Declined Bookings</h3>
              <% @denied_bookings.each do |booking| %>
              <div class="space">
                <h2><a href = "/spaces/<%= booking.space_id %>"><%= booking.name %></a></h2>
                <p>£<%= booking.price %></p>
                <p>Booking declined for <%= booking.booking_date %></p>
              </div>
              <% end %>
            </div>
          </div>
        </div>
      </header>
</body>

</html>